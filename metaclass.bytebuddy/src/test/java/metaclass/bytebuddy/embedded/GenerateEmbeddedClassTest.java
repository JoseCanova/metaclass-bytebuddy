package metaclass.bytebuddy.embedded;

import org.junit.jupiter.api.Test;

/**
 * Purpose of the test, provide means to generate a Embedded Class when during the 
 * RdbmsSchema MetaData where identified by means of classification a Metaclass which 
 * it's identity is classified as Composite.
 * 
 */
public class GenerateEmbeddedClassTest {

	
	@Test
	void testGenerateEmbeddedTypeClass() {
		
	}
}
